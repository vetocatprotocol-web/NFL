[package]
name = "nfl-schema"
version = "0.1.0"
edition = "2024"
publish = false
description = "NFL schema metadata and serialization primitives."
license = "Apache-2.0 OR MIT"

[dependencies]
nfl-core = { path = "../nfl-core" }
